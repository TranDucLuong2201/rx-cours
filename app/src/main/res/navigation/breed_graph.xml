<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/breed_graph"
    app:startDestination="@id/breedFragment">

    <fragment
        android:id="@+id/breedFragment"
        tools:layout="@layout/fragment_breed"
        android:name="com.rxmobileteam.pet.presentation.breed.BreedFragment"
        android:label="Breed">
        <action
            android:id="@+id/action_breedFragment_to_detailFragment"
            app:destination="@id/detailFragment" />
    </fragment>

</navigation>
